var variaVar = 4;
let variaLet = 2;
const constante = "constante"
console.log(`
    ${variaVar} (usando var)
    ${variaLet} (usando let)
    ${constante} (usando const)
    ${variaVar + variaLet}
    `
)